# Copy these values to Vercel Environment Variables; never commit a real password.
# Gmail uses an App Password, not the normal account password.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USE_SSL=true
SMTP_USERNAME=akshayush007@gmail.com
SMTP_PASSWORD=
SMTP_FROM=akshayush007@gmail.com
